pre-commit:
#   parallel: true
#   jobs:
#     - name: biome-check
#       glob: "*.{js,ts,jsx,tsx,json}"
#       run: pnpm biome check --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
#     - name: type-check
#       glob: "*.{ts,tsx}"
#       run: npx turbo type-check --filter=...[HEAD]

# commit-msg:
#   jobs:
#     - name: conventional-commit
#       run: node scripts/verify-commit-msg.cjs {1}
