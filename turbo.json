{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["API_URL", "API_TIMEOUT", "NODE_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "build/**"],
      "env": ["NEXT_PUBLIC_*", "EXPO_PUBLIC_*"]
    },
    "build:weapp": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "build:h5": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": { "cache": false, "persistent": true },
    "lint": {},
    "type-check": {},
    "clean": { "cache": false }
  }
}
